<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.wifi.attendance">

    <!-- Wi-Fi scan + location (Required since Android 6–14) -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.NEARBY_WIFI_DEVICES"
        android:usesPermissionFlags="neverForLocation" />

    <!-- Run foreground service -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <!-- Android 10+ foreground type: location -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />

    <!-- WakeLock for keeping screen/CPU active -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Boot complete if you use alarm after reboot -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.RECEIVE_EXACT_ALARM" tools:ignore="ProtectedPermissions" />

    <!-- Android 13+ notifications -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_wifi"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_wifi"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">

        <!-- Launcher -->
        <activity
            android:name=".SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- App Activities -->
        <activity android:name=".MainActivity" />
        <activity android:name=".WifiActivity" android:exported="false" />
        <activity android:name=".HistoryActivity" android:exported="false" />
        <activity android:name=".AlarmSettingsActivity" android:exported="false" />
        <activity android:name=".ScanHistoryActivity" />


        <!-- Boot receiver (reschedules alarms) -->
        <receiver
            android:name=".BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- Alarm broadcast receiver -->
        <receiver
            android:name=".WifiSchedulerReceiver"
            android:enabled="true"
            android:exported="true" />

        <!-- Background Wi-Fi scan service -->
        <service
            android:name=".WifiScanService"
            android:enabled="true"
            android:exported="false" />
    </application>
</manifest>
